import { Fragment } from "react";
import { Pred } from "../styled";

const Landing = () => {
    return (
        <Fragment>
            <Pred>Debes registrate para acceder a la web</Pred>
        </Fragment>
    );
}
export default Landing;
